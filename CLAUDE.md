# CLAUDE.md — 「데이터 과학」 웹 교과서 프로젝트 지침

이 파일은 이 저장소에서 작업하는 모든 에이전트·기여자가 **작업 시작 전에 반드시 읽는** 규칙서입니다.
설계 근거는 [docs/PLAN.md](docs/PLAN.md), 레퍼런스 분석은 [docs/reference-analysis.md](docs/reference-analysis.md)에 있습니다.

---

## 1. 프로젝트 목적

2022 개정 교육과정 고등학교 진로선택 과목 **「데이터 과학」(4단위, 총 64차시)** 의 웹 교과서를 만든다.

**세 가지를 동시에 만족해야 한다.**

1. **교육과정 준거성** — 사이트의 뼈대는 출판사 교과서가 아니라 **성취기준 19개**다. 모든 차시는 성취기준 코드에 1개 이상 매핑된다.
2. **교과서 중립성** — 학교가 YBM·씨마스·천재 중 어느 교과서를 쓰더라도, 학생이 "지금 배우는 내용이 내 교과서 몇 단원 몇 쪽인지" 찾아갈 수 있다. 출판사 자료는 **대조표(찾아가기)** 용도로만 쓴다.
3. **독립 저작물** — 본문·삽화·평가문항은 성취기준과 성취기준 해설을 근거로 **새로 쓴 것**이어야 한다. 출판사 교과서를 옮겨 적은 것이 아니다.

대상 독자는 **고등학생**이고, 실행 환경 기준은 **학교 전산실 크롬**이다.

---

## 2. 디렉터리 구조

```
D:\project_AI\22hs-ds\          ← 저장소 루트
├─ CLAUDE.md                    ← 이 파일 (작업 규칙)
├─ docs/
│  ├─ PLAN.md                   설계 계획서 (확정)
│  ├─ reference-analysis.md     레퍼런스 사이트 분석
│  └─ validation-report.md      검증 스크립트 출력 (자동 생성, 손으로 고치지 않는다)
├─ content/                     ★ 콘텐츠의 유일한 원본
│  ├─ curriculum.json           교육과정 — 영역 4, 성취기준 19 (원문·해설·고려사항)
│  ├─ publishers.json           출판사 대조표 (3사 단원 구조 + 성취기준 매핑)
│  └─ lessons/
│     ├─ 01-01.json  …  01-04.json      영역 Ⅰ
│     ├─ 02-01.json  …  02-04.json      영역 Ⅱ
│     ├─ 03-01.json  …  03-06.json      영역 Ⅲ
│     └─ 04-01.json  …  04-05.json      영역 Ⅳ
├─ scripts/
│  └─ validate.py               스키마·매핑·[확인필요] 검증
└─ site/                        웹 사이트 (Astro). 세부 구조는 site/README.md
   ├─ src/                      lib · layouts · components · scripts · pages
   ├─ test/                     chart.test.mjs · pyrunner.test.mjs
   ├─ dist/                     빌드 산출물 (정적 25페이지, 생성물)
   └─ serve.py                  배포본을 파이썬만으로 여는 스크립트
```

### 출판사 자료는 이 저장소 안에 없다

출판사 교과서·지도서·PPT는 **저작물이므로 저장소에 두지 않는다.** 원본은 아래에 있다.

```
D:\OneDrive - 와우고등학교\2. class\[교과서+지도서]\22개정 데이터과학\
   ├─ YBM/  씨마스/  천재/
   ├─ 고등_데이터 과학(설치형)/
   └─ *.pdf  *.pptx
```

**집필 중에는 이 자료를 열지 않는다**(§3-2, §6-1). 대조표에 필요한 단원명·쪽수·성취기준 배치는
이미 `content/publishers.json`에 전사돼 있으므로 다시 열 필요가 없다. 추가 확인이 필요하면
자료를 직접 열기 전에 담당 교사에게 묻는다.

### 파일 규칙

- **차시 파일명 = 성취기준 코드의 뒤 5자**(`01-02.json`). 차시 제목이 바뀌어도 파일명·URL은 바뀌지 않는다.
- `content/lessons/*.json`은 **차시 1개 = 파일 1개**다. 여러 에이전트가 병렬로 작업해도 충돌하지 않는다.
- `lessons.json`(통합본)은 빌드 시 생성되는 파생물이다. **손으로 만들지 않는다.**
- `curriculum.json`은 **교육과정 원문이므로 집필 중 수정 금지**다. 오탈자를 발견하면 수정하지 말고 보고한다.

---

## 3. 콘텐츠 작성 규칙

### 3-1. 성취기준 매핑은 필수다

- 모든 차시 JSON에 `"standards": ["12데과01-02", …]` 가 있어야 한다. **빈 배열은 오류다.**
- 코드는 `curriculum.json`에 실재하는 것만 쓴다. 존재하지 않는 코드는 검증에서 실패한다.
- 본문을 쓸 때 **근거의 순서**를 지킨다.
  1. 성취기준 원문 (`standards[].text`)
  2. 성취기준 해설 (`standards[].commentary`) — 있는 경우
  3. 성취기준 적용 시 고려사항 (`areas[].considerations`)
- 성취기준에 없는 내용을 주요 개념으로 끌어오지 않는다. 확장하고 싶으면 `callout`의 `variant: "tip"`이나 심화 블록으로 분리하고, **그것이 성취기준 범위 밖이라는 것을 드러낸다.**
- 한 차시가 성취기준 2개 이상을 담을 수는 있으나, **성취기준 1개가 어느 차시에도 안 들어가면 안 된다.** 검증 스크립트가 커버리지를 확인한다.

### 3-2. 교과서 원문 전재 금지

- 출판사 교과서·지도서의 **설명 문장, 예시, 삽화, 도표, 평가문항을 그대로 또는 조금 바꿔서 옮기지 않는다.**
- 출판사 자료에서 가져와도 되는 것은 **단원·차시 제목과 쪽수**뿐이다. 이것은 "찾아가기"를 위한 서지 정보이고 `publishers.json`에만 들어간다.
- 예시·데이터·문항은 **새로 만든다.** 소재는 학생 생활(급식·교통·기후·학교 활동)이나 공개 데이터에서 가져온다.
- 잘 알려진 표준 예시(붓꽃 데이터, 장바구니 분석 같은 교과서 공통 소재)는 개념 설명에 써도 되지만, **문장과 수치는 우리가 새로 쓴다.**
- 인용이 꼭 필요하면 짧게 인용하고 출처를 밝힌다. 다만 **교과서 본문 인용은 하지 않는다.**

### 3-3. 확인되지 않으면 `[확인필요]`

- 사실을 확인할 수 없으면 **추측해서 채우지 않는다.** 값 자리에 `"[확인필요]"` 문자열을 넣거나, 텍스트 안에 `[확인필요]` 표시를 남긴다.
- 특히 다음은 절대 추측하지 않는다.
  - 출판사 단원명·쪽수·저자
  - 성취기준 ↔ 출판사 소단원 매핑
  - 통계 수치, 연도, 기관명, 인물명, 법령·정책 이름
  - 이미지 파일 경로 (아직 준비된 이미지가 없다)
- `[확인필요]`는 **부끄러운 것이 아니라 정상적인 산출물**이다. 검증 스크립트가 목록으로 뽑아 후속 작업 항목이 된다.
- 사실 확인이 안 된 내용을 그럴듯하게 쓰는 것이 이 프로젝트에서 가장 나쁜 실패다.

### 3-4. 차시 JSON 스키마

```jsonc
{
  "id": "01-02",                       // 파일명과 동일
  "areaId": 1,                         // 1~4
  "order": 2,                          // 영역 안에서의 순서
  "title": "정형과 비정형, 그리고 속성의 가치",
  "lead": "2~3문장. 이 차시가 무엇을 다루는지.",
  "standards": ["12데과01-02"],        // 1개 이상. 필수
  "periods": "3~4차시",                // 권장 실제 수업 차시
  "objectives": ["…할 수 있다.", "…", "…"],   // 2~4개
  "keywords": ["정형 데이터", "비정형 데이터"],
  "blocks": [ /* 아래 블록 타입 */ ],
  "status": "draft"                    // 집필 직후는 항상 draft
}
```

**블록 타입** (`blocks[]`의 각 원소는 `type`을 가진다)

| type | 필수 필드 | 용도 |
|---|---|---|
| `prose` | `md` | 설명 문단. 제한된 마크다운(`**강조**`, `` `코드` ``, 문단 구분) |
| `heading` | `level`(2\|3), `text` | 본문 소제목. 우측 목차에 잡힌다 |
| `callout` | `variant`, `label`, `md` | `variant`: `concept`(개념 확정) \| `warn`(주의·함정) \| `tip`(요령·연결) \| `activity`(활동) |
| `term` | `term`, `desc` | 용어 정의. 본문 툴팁 + 용어집에 자동 수집 |
| `table` | `head[]`, `rows[][]` | 표. `caption` 권장 |
| `chart` | `kind`, `data`, `x`, `y` | `kind`: `scatter`\|`line`\|`bar`\|`hist`\|`box`. `xLabel`/`yLabel` 권장, `caption` 선택(§3-7) |
| `code` | `lang`, `source`, `expect` | 파이썬 실행 블록. `expect`는 예상 출력(오프라인 폴백용, **필수**) |
| `quiz` | `items[]` | 확인 문제. 아래 계약 |
| `figure` | `src`, `alt`, `caption` | 이미지. **준비된 이미지가 없으면 `src`는 `"[확인필요]"`, `note`에 필요한 그림을 서술** |

`quiz.items[]`:

```jsonc
{ "q": "문항",
  "type": "mc" | "ox",
  "choices": ["…", "…", "…", "…"],   // mc만
  "answer": 1,                        // mc=정답 인덱스(0부터), ox=true|false. 필수
  "explain": "해설. 왜 그 답인지, 오답이 왜 틀렸는지." }
```

### 3-5. 차시 본문의 흐름

고정 템플릿을 강제하지는 않지만, 아래 흐름을 기본으로 한다.

1. **도입** — 학생이 겪는 상황이나 질문에서 출발한다. `prose` + 필요하면 `callout(activity)`의 생각 열기
2. **개념** — `heading` → `prose` → `term` → `callout(concept)`로 개념을 확정한다
3. **적용** — `table` / `chart` / `code`로 실제 데이터를 만져 본다
4. **주의** — `callout(warn)`으로 흔한 오해나 함정을 짚는다
5. **평가** — `quiz` 3~4문항 (객관식 + OX 혼합)

**블록 순서 원칙**: 조작(`chart`·`code`) 뒤에는 반드시 그 결과를 말로 확정하는 `callout(concept)`이나 `prose`가 온다. 조작만 하고 끝나는 블록을 두지 않는다.

### 3-6. 분량 기준

차시 1개당 `blocks` **12~24개**, `quiz` 3~4문항, `term` 2~5개. 지나치게 짧으면 성취기준을 다 못 담고, 지나치게 길면 한 차시에 안 끝난다.

> 상한을 20에서 24로 올린 이유: 첫 집필에서 네 영역 모두 20에 걸렸고, 그 때문에 `callout(activity)`와 `figure`가 빠지는 일이 생겼다. **분량 상한 때문에 활동이나 삽화를 버리지 않는다.** 성취기준 해설이 두 축을 요구하는 차시(예: `12데과01-03`, `12데과02-04`)는 24개까지 쓸 수 있다.

### 3-7. `chart` 블록 데이터 규약

블록마다 다르게 쓰면 렌더러를 만들 수 없다. 아래로 통일한다.

```jsonc
{
  "type": "chart",
  "kind": "scatter",
  "data": [ { "시간": 2, "모금액": 18000 }, { "시간": 3, "모금액": 27000 } ],
  "x": "시간",                    // data 각 원소의 키 이름
  "y": "모금액",
  "xLabel": "운영 시간(시간)",
  "yLabel": "모금액(원)",
  "caption": "동아리 부스 운영 기록. 수치는 예시로 만든 값이다."
}
```

- `data`는 **객체 배열**이고, `x`·`y`는 그 객체의 **키 이름**이다. 한글 키를 허용한다.
- `kind: "hist"`는 원자료가 아니라 **미리 센 계급별 도수**를 값으로 적는다. `x`는 계급 라벨 키, `y`는 도수 키다.
- `kind: "box"`는 `data`에 요약값(최솟값·1사분위수·중앙값·3사분위수·최댓값) 키를 갖는 객체를 넣는다.
- **`caption`에는 데이터의 출처나 성격을 반드시 밝힌다.** 직접 만든 값이면 "예시로 만든 값이다"라고 적는다.
- 데이터를 코드로 생성하지 않는다(§6-10).

---

## 4. 문체 규칙

### 4-1. 기본

- 대상은 **고등학생**이다. **평서문(`~이다`, `~한다`)** 으로 쓴다.
- `~해요`, `~죠`, `~봅시다` 같은 구어체를 쓰지 않는다.
- 학생을 부를 때는 `여러분`을 쓰지 않는다. 필요하면 주어를 생략하거나 `우리`를 쓴다.
- 한 문장은 한 가지만 말한다. 40자 안팎을 기준으로 하고, 60자를 넘기면 끊는다.
- 과장·감탄을 쓰지 않는다(`놀랍게도`, `무려`, `대단한`). 이모지는 `callout`의 `label`에만 허용한다.

### 4-2. 전문용어

- **전문용어는 첫 등장 시 괄호로 원어나 풀이를 병기한다.**
  - `정규화(normalization)`, `결측치(missing value)`, `군집 분석(clustering)`
  - 약어는 풀어 쓴 뒤 약어를 병기한다 — `탐색적 데이터 분석(Exploratory Data Analysis, EDA)`
- 같은 차시 안에서 두 번째부터는 병기하지 않는다.
- 개념을 처음 정의할 때는 `term` 블록으로 만들어 용어집에 남긴다.
- 교육과정 용어를 임의로 바꾸지 않는다. 성취기준이 `향상도`라고 쓰면 `리프트`가 아니라 `향상도(lift)`로 쓴다.

### 4-3. 수와 단위

- 수식은 텍스트로 쓴다 — `정규화 값 = (값 − 최솟값) ÷ (최댓값 − 최솟값)`. LaTeX을 쓰지 않는다.
- 빼기는 유니코드 마이너스(`−`), 나누기는 `÷`를 쓴다.
- 단위는 숫자와 붙여 쓴다(`15cm`, `72점`). 천 단위 구분은 쉼표(`1,280`).

### 4-4. 쓰지 않는 표현

| 쓰지 않음 | 대신 |
|---|---|
| 데이터를 씻는다 / 청소한다 | 전처리한다 |
| AI가 알아서 | 모델이 데이터에서 규칙을 찾아 |
| 정답은 없다 | 문제와 목적에 따라 다르다 |
| 무조건 / 절대로 (강조용) | 조건을 명시한다 |

---

## 5. 커밋 규칙

### 5-1. 형식

```
<type>(<scope>): <요약>
```

- `type`: `content`(차시 집필) · `data`(curriculum/publishers) · `feat` · `fix` · `docs` · `chore`
- `scope`: 성취기준 코드(`01-02`) 또는 영역(`area1`) 또는 모듈명(`chart`, `validate`)
- 요약: **한국어 평서문, 50자 이내, 마침표 없음**

예:
```
content(01-02): 정형·비정형 데이터 차시 초안 작성
data(curriculum): 영역 Ⅲ 성취기준 해설 추가
fix(validate): 성취기준 코드 정규식이 04-05를 놓치던 문제 수정
docs(plan): 기술 스택 B안 확정 반영
```

### 5-2. 커밋 단위

- **차시 1개 = 커밋 1개**를 기본으로 한다. 여러 차시를 한 커밋에 묶지 않는다.
- `content/` 변경과 `scripts/`·`docs/` 변경을 같은 커밋에 섞지 않는다.
- 검증 스크립트가 실패하는 상태로 커밋하지 않는다. 미완이면 `status: "draft"`로 두고 커밋한다.

### 5-3. 본문에 적을 것

차시 집필 커밋에는 아래를 적는다.

```
content(03-04): 연관 분석 차시 초안 작성

- 성취기준: 12데과03-04
- 근거: 성취기준 원문 + 해설(장바구니 분석, 지지도·신뢰도·향상도)
- [확인필요] 2건: 예시 거래 데이터 출처, 삽화
```

---

## 6. 절대 하지 말 것

1. **출판사 교과서·지도서의 본문·삽화·평가문항을 옮기지 않는다.** 표현을 바꿔 쓰는 것도 전재다.
2. **모르는 사실을 그럴듯하게 채우지 않는다.** 통계·연도·기관명·쪽수·매핑은 확인되지 않으면 `[확인필요]`다.
3. **성취기준 없는 차시를 만들지 않는다.** `standards`가 빈 배열이면 그 차시는 존재 이유가 없다.
4. **`curriculum.json`의 성취기준 원문을 수정하지 않는다.** 교육과정 고시 원문이다.
5. **`quiz`에 `answer`를 빼놓지 않는다.** 레퍼런스 사이트에서 실제로 발생한 결함이고, 학생이 정답을 맞힐 수 없게 된다.
6. **`figure`에 존재하지 않는 이미지 경로를 적지 않는다.** 준비된 이미지가 없으면 `"[확인필요]"`다.
7. **`docs/validation-report.md`를 손으로 고치지 않는다.** 스크립트 출력물이다.
8. **다른 에이전트가 담당한 영역의 파일을 건드리지 않는다.** 자기 영역의 `content/lessons/*.json`만 쓴다.
9. **구어체로 쓰지 않는다.** §4를 따른다.
10. **`chart` 블록에서 데이터를 코드로 생성하지 않는다.** 데이터는 블록에 값으로 적는다. (레퍼런스는 리사이즈 때 데이터를 재생성해 통계값이 바뀌는 버그가 있었다.)
11. **성취기준 범위를 넘는 내용을 주요 개념으로 넣지 않는다.** 딥러닝·신경망은 이 과목의 성취기준에 없다.
12. **학생 개인정보를 다루는 예시를 만들지 않는다.** 실명·연락처·성적 실데이터를 소재로 쓰지 않는다.
